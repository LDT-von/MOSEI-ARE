# 历史 SlotEmo 实验

[`SlotEmo_experiments/`](SlotEmo_experiments/README.md) 保存了早期基于附件2 `aligned_50.pkl` 的三次模型训练探索。旧目录从数据文件夹整体迁到这里；源码与文字结果可纳入 Git，三个最佳权重仅在本机 `outputs/` 中保存。

迁入时只整理了硬编码路径和误导性的脚本标题，没有重新训练或更改模型计算。旧实验不是问题一的原视频特征提取成果，也不是问题二、三的正式模型。正式问题一材料在 `problem1/`；以后若复用旧模型，应先移植到对应题目目录并重新验证。
