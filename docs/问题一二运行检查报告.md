# 问题一、二运行检查（2026-09-23）

结论：当前代码在本机完成了实际运行检查。后续由用户自行启动实验，助手不再自动启动训练。检查环境为 Windows、Python 3.10、PyTorch 2.6.0+cu124，显卡为 RTX 3060 Laptop GPU。尚未在用户的 Linux 电脑验证其驱动和依赖环境。

| 检查 | 实际结果 | 证据边界 |
| --- | --- | --- |
| 问题一时序单元测试 | 6 项通过 | 验证区间汇总、尾段、来源文字和静音处理 |
| 问题一全部现有输出 | 100/100 样本通过来源哈希、标签、形状、有限值、时间轴与独立重建核验 | 不等于人工词边界准确率 |
| 问题一原视频重提取 | CPU 临时副本实际提取 1 条视频，生成28个时间格、384/33/512维特征并通过质量筛查流程，耗时17.26秒 | 已有交付特征未被改写 |
| 问题一压缩包 | 227 个文件、100 个 NPZ、18,140,419 字节；ZIP CRC 检查通过，未包含 qa/ 或 assets/ | 本地包已更新，ZIP/特征不进 Git |
| 问题二单元测试 | 8 项通过 | 覆盖缺口有效性、掩码隔离、全缺失、确定性、共享初始化和调试权重拒绝 |
| 问题二 A/B/C 完整试跑 | CPU 三组全部通过；每组32条训练样本、1 epoch、4次参数更新、16条验证样本 | 是小规模运行检查，分数不可写为正式性能 |
| 问题二 CUDA 完整试跑 | C 组在 RTX 3060 Laptop 上同样通过 | 验证本机 CUDA 路径，尚不证明 Linux 环境已配置正确 |
| 问题二验证与推理文件 | 每组29种验证条件成功；权重保存重载成功；附件3产生30行、30个唯一ID | 文件均带调试标志，不可提交 |

本次修复：同位补偿排除 `[CLS]`/`[SEP]` 占位；当候选窗口正好落在已有空洞中时选择最近的非空窗口；同随机种子下 A/B/C 的共享主干初始化与批次顺序保持一致；保存固定验证掩码和逐样本缺口位置；记录实际被遮挡样本数；正式推理拒绝调试权重。问题一人工复核报告也修正了计数，将“抽取30条”与“已复核0条”分开。

问题一自动筛查仍为79条通过、21条保留但屏蔽默认时序文本。人工表抽取了30段视频、90个词，目前没有已填写的人工作答，因此人工时间误差仍为空。问题二正式训练和测试集性能尚未取得；小型文本编码器从词元训练的效果仍需正式实验验证。

曾启动一次 C 组全训练集运行，在记录5个 epoch 后按用户要求中止；已确认没有该训练进程继续运行。未完成产物保留在 `problem2/qa/interrupted_C_seed42/` 并写入不可提交标记，不作为正式结果，不推送。用户重新开始时使用新的正式输出目录。

## 可复现检查

从仓库根目录运行：

```text
python problem1/test_alignment.py
python problem1/verify_outputs.py
python problem1/smoke_check.py --device cpu
python -m unittest problem2.test_pipeline -v
python -m problem2.scripts.run smoke --arm C --device cuda --output problem2/qa/linux_first_check
```

Linux 上先将 `MOSEI_DATA_ROOT` 设置为包含附件2、附件3目录的数据根。smoke 通过后再按 `problem2/README.md` 跑 A/B/C 正式训练。若希望重跑同一试跑，请换新的 `--output` 目录，避免覆盖已有证据。

本地详细记录：`problem1/qa/smoke_report.json`、`problem1/qa/smoke_check.log`，以及 `problem2/qa/smoke_A_cpu`、`smoke_B_cpu`、`smoke_C_cpu`、`smoke_C_cuda` 中的 `smoke_report.json`、运行配置、训练历史、验证掩码和调试预测。这些逐样本文件及调试权重均不推送。

当前 Python 环境在问题一启动时存在 requests 依赖版本警告，未阻断离线提取。新 Linux 环境应按各问题的 requirements 安装；本次未改动系统 Python 的全局依赖。
