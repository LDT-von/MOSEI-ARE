# 5090 端 AI 审查 · 提示词（v1）

> 用途：把整份项目交到 5090 上跑的 AI，做**端到端复现 + 质量审查**。
> 用法：把下面 `<task>` 标签之间的整段提示词连同仓库 URL 一起发给对方。

---

## 仓库与数据位置

- 仓库：`https://github.com/LDT-von/MOSEI-ARE.git`（clone 即可）
- 数据根目录（不要上传到 git）：用户本地 `C:\Users\栋栋\Desktop\E题\E题数据\E题数据\`，包含 4 个附件目录。AI 端请把这份路径写成 `MOSEI_DATA_ROOT` 环境变量。
- 提交物：`E题提交.zip`（论文 + 代码 + 预测/证据 JSON）位于用户桌面。
- 不需要原始视频帧或附件 1 全部特征文件就能跑问题 2/3；问题 1 跑需要附件 1。

## 已完成训练

仓库里 `problem2/outputs/arm_{A,B,C}_seed_42/best.pt` **不在仓库里**（`.pt` 在 `.gitignore` 里）。审查方要么自己训一遍，要么跟作者拿 `.pt`。训一组大约 10 分钟（RTX 3060 Laptop）；5090 应更快。

## 关键指标快照（用于快速对账）

```
problem2/outputs/arm_A_seed_42/test_evaluation.json:
  clean: acc=0.549, f1_macro=0.440, mae=0.836, pearson=0.395
  mixed: acc=0.539, f1_macro=0.436, mae=0.840
problem2/outputs/arm_B_seed_42/test_evaluation.json:
  clean: acc=0.514, f1_macro=0.460, mae=0.849
  mixed: acc=0.498, f1_macro=0.445
problem2/outputs/arm_C_seed_42/test_evaluation.json:
  clean: acc=0.492, f1_macro=0.449, mae=0.839
  mixed: acc=0.490, f1_macro=0.446
problem2/outputs/arm_C_seed_42/problem3_test_evaluation.json:
  accuracy=0.4924, f1_macro=0.4492, n=727
problem2/outputs/arm_C_seed_42/problem3_attachment4/attachment4_predictions.csv: 20 行
problem2/outputs/arm_C_seed_42/problem3_attachment4/attachment4_evidence.json: 完整证据
```

---

## 提示词正文

````
<task>
请你在 5090 上对一份多模态情感识别建模项目做**端到端复现 + 代码质量审查 + 结果合理性审查**。仓库已 clone 到工作目录；附件数据按下面"数据获取"步骤获得。

# 项目概览

这是中国全国大学生数学建模竞赛 E 题"复杂场景下多模态情感识别"的三问解答：
- 问题 1：附件 1（100 条原视频）的多模态特征提取与时序对齐
- 问题 2：附件 2（4850 条 train+valid+test，三模态各 50 位置）+ 附件 3（30 条无标签）的鲁棒情感预测
- 问题 3：附件 2 验证/测试集 + 附件 4（20 条无标签）的可解释预测与证据生成

模型主干是 `models/gap_slot_emo.py: GapSlotEmo`：三模态独立投影 → 每个模态一个 MaskedSlotAttention（`problem2/configs/default.json`：`slots=16`、`slot_heads=4`、3 次迭代）→ 模态门控拼接后经 MLP 融合 → 极性(3 类)/强度(1 维)双头。注意：模型内没有卷积层，时序信息来自位置嵌入；融合发生在 Slot Attention 之后。

# 数据获取

附件不在 git 里。请到作者说的网盘/渠道拉取附件 1-4，放到：
```
D:\attachments\
├── 附件1-数据集原始音视频\
├── 附件2-数据集特征文件\
├── 附件3-模态缺失特征样本\
└── 附件4-可解释专用视频和特征文件\
```
（路径可换，但请把 MOSEI_DATA_ROOT 设为相应根目录。）

# 你需要完成的事情

## 第 1 步：环境准备（5 分钟）

```bash
cd <repo>
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install transformers av opencv-python pandas scikit-learn openpyxl numpy matplotlib pillow
# transformers 仅用于下载 BERT/ASR；问题 2/3 不需要远程权重
```

如果下载慢，请设置国内镜像；不要换 Python 版本（项目基于 3.10）。

## 第 2 步：检查文档

请打开并通读：
- `README.md`：总览
- `problem1/问题一建模与结果.md`
- `problem2/问题二建模与结果.md`
- `problem3/问题三建模与结果.md`
- `提交前检查清单.md`

报告：是否有事实错误、图表缺失、引用错误、明显的逻辑漏洞（不要抠英文）。

## 第 3 步：问题 1 特征提取（可选，成本最高）

注意：仓库里**没有** `problem1/exp3_problem1.py`。同名旧脚本只在 `legacy/SlotEmo_experiments/scripts/` 下，读的是附件2的 `aligned_50.pkl`，按 `problem1/问题一建模与结果.md` 的说法**不能用于证明问题一已完成**。正式入口是 `problem1/` 下的多脚本流水线，需要附件 1 原视频和预训练权重（`prepare_models.py` 会下载公开权重），原运行使用 CUDA 版 PyTorch——这一步不是"仅 CPU、几分钟能跑完"的任务。

```bash
cd problem1
python inventory.py --workspace <E题数据根目录>
python prepare_models.py
python pipeline.py --workspace <E题数据根目录>
python apply_quality_gate.py
python verify_outputs.py
python build_report.py
```

`--workspace` 指向含 `E题数据/E题数据` 的题目目录；`pipeline.py` 另有 `--device`、`--limit`（仅调试）与 `--resume`。只想起步验证可用 `python smoke_check.py --device cpu`。

期望（对应仓库里已有的产物）：
- `problem1/outputs/features/` 下有 100 份 NPZ（目录名是 `outputs/`，**不是** `output/`）
- `problem1/outputs/quality_report.json` 的 `samples_passing_automatic_alignment_screen` 为 79

报告：能否跑通、100 份特征是否齐全、筛查通过数是否为 79。若拿不到附件 1 或不想耗时，可直接核对上面两个已有产物，并明确说明本步跳过。

## 第 4 步：跑问题 2 训练（仅 Arm C，必须）

```bash
$env:MOSEI_DATA_ROOT = "D:\attachments"
python -m problem2.scripts.run train --arm C --seed 42 --device cuda --output problem2/outputs/arm_C_seed_42
```

期望：约 5-10 分钟结束，输出 `best.pt`、`history.json`、`valid_evaluation.json` 等。

请验证：
1. 训练曲线是否收敛（不应震荡或 loss=nan）
2. valid selection_score 最高 epoch 应在第 5-15 之间
3. 检查 `valid_evaluation.json` 里 27 grid 都有数值（不应有 nan）
4. 把作者给的指标（clean acc=0.492, f1=0.449）和你的结果对比，差值应 < 0.02（不同 GPU 浮点允许小幅波动）

## 第 5 步：跑问题 2 评估（针对已有或刚训出的 Arm C）

```bash
python -m problem2.scripts.run evaluate --checkpoint problem2/outputs/arm_C_seed_42/best.pt --split test --device cuda
python -m problem2.scripts.run evaluate --checkpoint problem2/outputs/arm_C_seed_42/best.pt --split valid --grid --device cuda
```

注意：仓库里没有预置 `best.pt`（被 `.gitignore` 忽略，见第 4 步说明），第 5、6 步的所有 `--checkpoint .../best.pt` 命令都必须先完成第 4 步重训、或从作者处取得 checkpoint，否则会直接报文件不存在。

报告：test clean / mixed / 27 grid 关键数字。

## 第 6 步：跑问题 3 推理

```bash
python -m problem3.scripts.run evaluate-valid --checkpoint problem2/outputs/arm_C_seed_42/best.pt --device cuda
python -m problem3.scripts.run evaluate-test  --checkpoint problem2/outputs/arm_C_seed_42/best.pt --device cuda
python -m problem3.scripts.run evidence-valid --checkpoint problem2/outputs/arm_C_seed_42/best.pt --sample-size 32 --device cuda
python -m problem3.scripts.run infer --checkpoint problem2/outputs/arm_C_seed_42/best.pt --device cuda --window-length 6 --window-top-k 3
python -m unittest problem3.test_pipeline -v
```

期望：8/8 单元测试通过；test F1 在 0.42-0.48 之间；附件 4 预测 CSV 共 20 行。

## 第 7 步：附件 3/4 提交文件检查

打开 `problem2/outputs/arm_C_seed_42/attachment3_predictions.csv` 和 `problem3_attachment4/attachment4_predictions.csv`：

1. 行数：30 / 20
2. 列：参考 `problem3/问题三建模与结果.md` 第 5 节
3. 极性分布：不应全是一类；附件 4 应有 0/1/2 三类
4. 强度范围：合理（[-3, +3]）

## 第 8 步：代码审查

请重点看：
- `models/gap_slot_emo.py`：网络结构、mask 处理、Slot Attention 实现
- `problem2/scripts/run.py`：训练循环、gap 注入逻辑、early stopping
- `problem3/explain.py`：模态反事实遮挡、窗口扫描
- `problem2/data.py`：数据加载、padding、normalization

特别检查：
1. **数据泄漏**：normalization 是否只用 train fit？gap 注入是否对 test 也合法？
2. **mask 一致性**：training 和 inference 时 `observed`、`support`、`padding` 是否有不一致？
3. **指标计算**：F1、neutral recall、MAE、Pearson 是否按 sklearn 实现？
4. **数值稳定性**：loss 是否可能 nan（输入异常大）？
5. **可复现性**：所有随机数是否都通过种子控制？
6. **附件 4 特殊处理**：附件 4 没有标签，证据扫描是否在 evidence 步骤里真的走完了 forward？

## 第 9 步：可解释性证据的合理性

打开 `attachment4_evidence.json` 里 sample_id=03 的 evidence（该样本在 `sample_index=2`）：
- `modality_contribution.dominant_modality = "vision"`，依据是 `normalized_magnitude.vision = 0.545`（三模态最大）与 `polarity_delta.vision = -0.654`
- 对应原视频是 `03.mp4`（`video_filename` 字段，命名规则就是 `<sample_id>.mp4`；`01.mp4` 属于 sample 01）
- `window_evidence.vision.windows[0]` 是 0.30-1.50s，该窗口 `logit_drop = 0.0794`
- 文本内容（`raw_text`）关于"Bankwest 房贷/租金收益"，模型对该样本预测 `polarity=2`（正类，不是中性）

注意别混淆：**0.632 是 text 模态 top 窗口（1.10-2.30s）的 `logit_drop`**，不是 vision 的；vision 窗口只有 0.079，仓库里也不存在名为 `vision_logit_drop` 的字段。

报告：证据窗口在语义上是否合理？如果 vision 在 0.3-1.5 秒确有面部表情或画面变化，vision 被判为主控模态是说得通的；但该窗口 `logit_drop` 仅 0.079，量级远低于 text 的 0.632，请判断"主控模态是 vision"与"移除该窗口对 logit 影响很小"之间的张力是否说明需要更严格的对照实验。

## 第 10 步：最终交付报告

请按以下模板给出审查报告：

```markdown
# 5090 端审查报告

## 1. 复现性
- 问题 1：[通过 / 失败 / 偏差，附实测数据]
- 问题 2 Arm C 训练：[通过 / 失败 / 偏差]
- 问题 2 测试评估：[通过 / 失败 / 偏差]
- 问题 3 推理：[通过 / 失败 / 偏差]
- 单元测试：[8/8 通过 / X/8]

## 2. 指标对账
（把作者的指标和你的实测值并列，差值 > 0.02 标红）

## 3. 代码质量（按问题点列）
1. [严重 / 重要 / 一般 / OK]：描述
2. ...

## 4. 文档质量
1. [严重 / 重要 / 一般 / OK]：描述
2. ...

## 5. 可解释性证据合理性
- 整体：X 条样本里有 Y 条证据在语义上能解释
- 异常 / 不可解释样本：[列出]
- 是否需要更严格的对照实验？

## 6. 已知遗留问题（不影响提交但应记录）
1. ...

## 7. 改进建议（可选，不阻塞）
1. ...

## 8. 总体评分
- 复现性：X/10
- 代码质量：X/10
- 结果合理性：X/10
- 总评：[可提交 / 小修后可提交 / 需大修]
```

# 重要约束

1. **不要修改任何代码**——只读审查 + 运行。如果你发现明显 bug，请列出但**不要**直接改。
2. **不要 push 到任何远程**——所有改动留在本地或 working tree。
3. **不要装额外的库**——如果某个 import 缺失，请报告。
4. **请在 5090 上跑，不要在 CPU 上跑问题 2/3 训练**（太慢）。
5. 报告结尾请附 `git log --oneline -5` 和 `git status` 输出，确认是仓库原始 commit。

# 沟通

- 任何不清楚的地方，先列出 3-5 个候选假设继续往下走，不要卡住问。
- 报告长度建议 200-500 行 Markdown；如果实在没事写就写 50 行；不要凑字数。

</task>
````

---

## 使用说明（给你）

1. 把上面 `<task>...</task>` 的整段（Markdown + code fence）复制发给 5090 那边的 AI（或写成大文档发给它）
2. 把仓库 URL + 数据来源提前告诉对方
3. 收到报告后，对"严重"和"重要"的问题做修复
4. **不要**让对方自己 push 或改代码

## 备选短版（如果对方 AI 不喜欢长 prompt）

如果你觉得上面太长，把这段短版先发过去，再决定要不要详细版：

```
请基于 GitHub 仓库 LDT-von/MOSEI-ARE（commit 221beda）做端到端审查：
1) 跑通 problem1 的特征提取流水线（cd problem1 && python pipeline.py
   --workspace <题目工作目录>，注意没有 problem1/exp3_problem1.py）、
   problem2/scripts/run.py train --arm C、problem3/scripts/run.py
   evaluate-test 与 infer（后两步需要先重训出 best.pt）；
2) 把作者记录的关键指标（test F1=0.449、acc=0.492，附件4 20行）
   和你的实测值做对比，差 > 0.02 标记为偏差；
3) 重点审查 models/gap_slot_emo.py、problem2/scripts/run.py、
   problem3/explain.py 的 mask 一致性、数据泄漏、normalization；
4) 检查附件4 evidence JSON 里 sample_id=03（dominant=vision，
   vision 窗口 0.30-1.50s 的 logit_drop=0.079，text 的 logit_drop=0.632）
   在原视频 03.mp4 里是否有合理的视觉证据；
5) 给出 200 行以内的 Markdown 报告，包含复现性 / 指标对账 /
   代码质量 / 证据合理性 / 总评分。
注意：不要修改代码，不要 push。数据根目录从作者那里取。
```
