# Shared experiment conventions

Keep only settings used by multiple questions here, such as the selected Attachment 2 release, common seed policy and shared label mapping. Each problem folder owns its task-specific configs and validation decisions.

Every result should identify the feature release, split, seed, model revision, objective, threshold rule and checkpoint. Do not use unlabeled special-test inputs to choose settings.
